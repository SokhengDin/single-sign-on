# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=sso_db
DB_SSLMODE=disable

# App
APP_URL=http://localhost:3004
APP_ENV=development
APP_PORT=3004

# Signing key encryption secret (AES-256-GCM)
# Generate with: openssl rand -base64 32
SIGNING_KEY_ENCRYPTION_SECRET=
